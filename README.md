# Info

### How to run:

1) Install `nodjs`

```bash
> choco install nodejs
```

2) Enter main directory that contains /src/ and install `npm` server.

```bash
> npm install
```

3) Run `npm` server.

```bash
> npm run dev
```